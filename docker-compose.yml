services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - waittime-data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=3000
      - WAITTIME_DB_PATH=/app/data/waittimes.db
    restart: unless-stopped

volumes:
  waittime-data:
